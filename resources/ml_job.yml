resources:
  jobs:
    underwriting_05_ml:
      name: underwriting_05_ml
      description: "Feature store → models + serving endpoints → crux decision functions → what-if functions"
      tags:
        project: underwriting_workbench
      parameters:
        - name: catalog
          default: ${var.catalog}
        - name: schema
          default: ${var.schema}
      environments:
        - environment_key: default
          spec:
            client: "5"
            dependencies:
              - databricks-feature-engineering
              - lightgbm
              - scikit-learn
              - mlflow
              - databricks-sdk
      tasks:
        - task_key: features
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/04_features.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
        - task_key: models
          depends_on:
            - task_key: features
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/05_models.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
        - task_key: crux
          depends_on:
            - task_key: features
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/05b_crux.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
        - task_key: whatif
          depends_on:
            - task_key: crux
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/05c_whatif.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
        # Lane E (additive): referral-event derivation fn (composes crux) → gold_referral_events
        - task_key: lane_e_crux
          depends_on:
            - task_key: crux
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/05d_lane_e_crux.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
        - task_key: gold_referral_events
          depends_on:
            - task_key: lane_e_crux
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/03c_gold_referral_events.py
            base_parameters:
              catalog: "{{job.parameters.catalog}}"
              schema: "{{job.parameters.schema}}"
